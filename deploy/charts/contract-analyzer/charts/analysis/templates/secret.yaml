{{- if .Values.secrets }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "analysis.fullname" . }}-secret
  labels:
    {{- include "analysis.labels" . | nindent 4 }}
type: Opaque
data:
  {{- toYaml .Values.secrets | nindent 2 }}
{{- end }}
