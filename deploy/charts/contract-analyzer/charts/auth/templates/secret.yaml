{{- if .Values.secrets }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "auth.fullname" . }}-secret
  labels:
    {{- include "auth.labels" . | nindent 4 }}
type: Opaque
data:
  {{- toYaml .Values.secrets | nindent 2 }}
{{- end }}
